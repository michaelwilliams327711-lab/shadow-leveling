"I want you to perform a 'Total System Sovereign' deep-scan audit of the entire Shadow Leveling project. This is a read-only diagnostic. Do not modify any code. Generate a 'System Health Ledger' categorized by severity: [CRITICAL], [HIGH], [MEDIUM], and [LOW].Analyze the project across these five specialized domains:1. The 'Temporal Integrity' Domain (Timezones & Crons):Audit every instance of new Date(). Identify where the server and client might disagree on the definition of 'Today'.Check if the midnight cron job (auto-processing overdue/recurring quests) is hardcoded to a specific timezone (like ET) or if it correctly respects the user's local offset.Find any 'Temporal Rifts' where a quest completed at 11:59 PM local time might be erroneously stamped as tomorrow in UTC.2. The 'Mechanic Drift' Domain (Math & Logic):Compare the leveling formula $100 \times \text{level}^{1.5}$ across all routes (quests.ts, daily-orders.ts, bosses.ts). Identify any hand-rolled loops that duplicate what processLevelUp.ts should be doing.Audit the 'Reward Authority'. Is there any 'Ghost Data' in the API contract (like isDaily or xp_reward) that doesn't actually exist in the database?Verify that Boss victories grant proportional stats and follow the same logic as high-rank Quests.3. The 'Data Scalability' Domain (Performance):Scan for N+1 queries (loops that fire individual SQL selects) and Unbounded SELECTs (fetching the entire table without a WHERE or LIMIT).Specifically check the GET /quest-log and GET /activity endpoints. Will these crash or lag after 5 years (2,000+ entries)?Identify $O(n^2)$ algorithms—specifically linear .find() or .filter() calls inside a map that iterates over 30+ days.4. The 'React & Memory' Domain (UI Stability):In the 1,800-line Quests.tsx and the DailyOrders.tsx components, check for useEffect hooks without cleanup functions.Audit the Yearly Heatmap. Are we mounting 365 individual Tooltip trees? If so, recommend a more performant rendering strategy.Look for 'Prop-to-State' anti-patterns (syncing props with useEffect) that cause double-renders or flickers.5. The 'Consolidation' Domain (Redundancy):Identify overlapping systems. Can 'Daily Orders', 'Quests', and 'Bad Habits' be unified into a single 'Task Engine'?Look for 'Double-Fetching' where the same page makes two separate API calls for the same data (e.g., the Planner vs. Daily Orders).Format the results into a high-level table followed by detailed descriptions for every vulnerability found. End with a 'Priority Fix Order' list."