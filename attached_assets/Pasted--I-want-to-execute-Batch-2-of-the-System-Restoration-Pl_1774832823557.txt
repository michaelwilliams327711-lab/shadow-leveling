"I want to execute Batch 2 of the System Restoration Plan. This is a backend optimization and database cleanup strike. Please implement the following:1. SQL-Side Filtering (GET /quests):Update the GET /quests route to use a SQL WHERE clause to filter by status and date range directly in the database.Stop fetching every quest ever created just to filter them in JavaScript. This is critical for 30-year scalability.2. Remove Redundancy (Database Schema):Delete the isDaily column from the quests table.Update all write/read paths to rely solely on the recurrence.type === 'daily' logic. This eliminates a "denormalized" field that is a risk for data drift.3. Unified Stat Mapping:Unify CATEGORY_STAT_MAP (frontend) and CATEGORY_STAT_GAINS (backend) into a shared constant or a single source of truth to ensure stat assignments never diverge.4. Loop Guardrails (Safety):Add a hard iteration cap (e.g., 100) to the while loops for level-up processing and getNextDueFromNow calculations. This prevents the server from hanging if it encounters corrupt or massive data imports.5. Character Cache:Implement a simple in-memory cache for getOrCreateCharacter() with a 2-second TTL (Time-To-Live). This prevents redundant database hits during rapid-fire requests (like completing multiple quests at once).Constraint: Do not change any UI components or CSS. Focus strictly on these backend performance and schema improvements."Why Batch 2 is the "Engine Overhaul"Database vs. JavaScript: Right now, your app is like a librarian who brings you every single book in the library just so you can pick one. By adding SQL WHERE clauses, the librarian only brings you the exact book you asked for. This saves massive amounts of memory.The "O(n)" Factor: By removing $O(n^2)$ loops (where the app has to look through every item for every other item), the dashboard will feel "snappy" even when you have 10,000 completed quests in your history.Data Integrity: Removing the isDaily column means there is only one place where the "truth" about a quest's frequency lives. If you change it in one place, it's changed everywhere.