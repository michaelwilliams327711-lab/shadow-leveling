MASTER SPECIFICATION: PROJECT SHADOW LEVELINGROLE: Act as a Senior Full-Stack Engineer and Game Systems Architect specializing in React, Express, and PostgreSQL.PROJECT CORE:Build a gamified productivity dashboard titled "Shadow Leveling," inspired by the Solo Leveling franchise. The system must function as a high-performance "Real-World RPG System."1. ARCHITECTURE & SECURITY (MANDATORY AUDIT FIXES):Security: Implement verifyToken using HMAC-SHA256 normalization (hash incoming tokens and keys to 32-byte digests) before calling crypto.timingSafeEqual to eliminate all timing-side-channel leaks.CORS: Enforce strict CORS_ORIGIN matching from environment variables. If NODE_ENV is production and CORS_ORIGIN is missing or *, the app must process.exit(1).Database Integrity: Every entry in quest_log and penalty_log MUST include a non-nullable characterId (Foreign Key to the character table). No orphaned logs allowed.Concurrency: Wrap all XP gains, gold updates, and daily resets in database transactions using .for("update") row-level locking to prevent race conditions.2. DATA LOGIC:Leveling Formula: $Level = \sqrt{TotalXP / 10}$.Quest Filter: The "Active Quests" logic must return all quests where status === 'active'. This includes recurring tasks AND one-off tasks with no specific deadline.Timezone Sync: Use process.env.LOCAL_TZ_OFFSET (default to -5 for EST) to calculate the "Daily Arise" reset window.3. UI/UX SPECIFICATIONS:Theme: "The System" Aesthetic. Black/Charcoal background, electric purple (#8b5cf6) highlights, and gold trim.Key Components:Status Window: Level, XP Progress Bar, Gold count, and "Daily Arise" button.Attribute Radar Chart: 6-point hexagonal chart for STR, SPI, END, INT, and DIS.Hunter’s Log: A 365-day activity heatmap (GitHub-style) visualizing quest completion.Tech Stack: React (Vite), Tailwind CSS, Lucide-React icons, Recharts for data viz.4. INITIALIZATION TASK:Build the Status Window and Radar Chart first. Connect them to a PostgreSQL backend using Drizzle ORM. Ensure the backend handles the level math correctly and the frontend doesn't "ghost" one-off quests.