I am authorizing Strike 3: The Dashboard Reconstruction. We are building the high-fidelity 'Scale of Souls' UI and the necessary CSS animations. Execute exactly as specified:

1. CSS Atmosphere (artifacts/shadow-leveling/src/index.css):
Add these animations to the bottom of the file. These are essential for the 'Solo Leveling' aesthetic:

CSS
@keyframes domainDrift {
  0%   { transform: translate(0px, 0px) rotate(0deg) scale(1); }
  50%  { transform: translate(4px, -8px) rotate(0.5deg) scale(1.01); }
  100% { transform: translate(0px, 0px) rotate(0deg) scale(1); }
}
@keyframes siegePulse {
  0%, 100% { opacity: 0.8; transform: scaleX(1); }
  50% { opacity: 1; transform: scaleX(1.01); }
}
@keyframes glitchHigh {
  0% { transform: translate(0,0) skew(0deg); clip-path: inset(0 0 100% 0); }
  10% { transform: translate(-2px, 2px) skew(2deg); clip-path: inset(10% 0 60% 0); }
  20% { transform: translate(2px, -2px) skew(-2deg); clip-path: inset(40% 0 30% 0); }
  100% { transform: translate(0,0) skew(0deg); clip-path: inset(0 0 0 0); }
}
@keyframes ariseMana {
  0% { opacity: 0; transform: translateY(0px) scale(0.6); }
  100% { opacity: 0; transform: translateY(-30px) scale(1.2); }
}
@keyframes corruptionSmoke {
  0% { opacity: 0; transform: translateY(0px) scale(1); }
  100% { opacity: 0; transform: translateY(20px) scale(1.5); filter: blur(2px); }
}

.animate-domain-drift { animation: domainDrift 15s ease-in-out infinite; }
.animate-siege-pulse { animation: siegePulse 2s ease-in-out infinite; }
.animate-glitch-high { animation: glitchHigh 0.4s steps(1) forwards; }
2. The Battlefield Component (artifacts/shadow-leveling/src/pages/CelestialDuel.tsx):
Create this file. It must implement the 7 Domain Pairs (Lust, Gluttony, Greed, Sloth, Wrath, Envy, Pride) and use the /api/ascension/powers and /api/ascension/quick-log endpoints. The UI should feature:

TugBar Component: A bar that shifts its center based on Virtue/Vice scores.

Particle Effects: arise-mana for Virtue gains, corruption-smoke for Vice gains.

Great Fall Logic: If a log response returns greatFall: true, trigger a screen-shaking glitch effect using the animate-glitch-high class.

3. App Integration:

Ensure CelestialDuel is imported and routed in App.tsx at the path /celestial.

Add 'Scale of Souls' to the AppSidebar.tsx navigation.

'Sovereign Format' Reporting Protocol (REQUIRED):

NO SUMMARIES: I need the raw, line-by-line source code of CelestialDuel.tsx.

MARKDOWN HEADERS: Every file must begin with a clear ### FILE: [Full/Path/To/File] header.

RAW CODE BLOCKS: Wrap all code in triple-backtick Markdown blocks.

FINAL INVENTORY: End with an 'Inventory Check' confirming the /celestial route is active in the frontend."