"I want to resolve every issue identified in the Post-Stability-Strike Audit Report. Please execute the following fixes across the entire stack. This is a full-system optimization and bug-fix sweep.1. Accuracy & Logic (Critical/High)XP Progress Math: In Dashboard.tsx, fix the xpPercent formula. Use:$$\text{xpPercent} = \text{Math.round}\left(\frac{\text{character.xp}}{\text{character.xpToNextLevel}} \times 100\right)$$Race Condition: In api-server/src/routes/character.ts, replace the SELECT/INSERT/UPDATE block for activityTable with a single INSERT ... ON CONFLICT DO UPDATE statement.Heatmap Off-by-One: Standardize the heatmap to 364 days in both character.ts and dashboard.ts loops to match the frontend.Monthly Recurrence: Ensure the Day-31 clamping logic we discussed is fully implemented in quests.ts.2. Performance & React Best Practices (High/Medium)Heatmap Optimization: In Heatmap.tsx, build a Map<string, ActivityDay> for the activity data to replace the $O(n^2)$ .find() lookup inside the day loop.Analytics Migration: Migrate ShadowDashboard.tsx and AnalyticsDashboard.tsx from raw fetch() to TanStack Query hooks. Implement AbortController for any remaining manual fetches to prevent memory leaks.React Keys: Replace all index-based key={i} props in AnalyticsDashboard.tsx, PenaltyModal.tsx, and Heatmap.tsx with stable unique identifiers (like .id, .stat, or .dateStr).Tooltip Memoization: Wrap flipX and clampY in useCallback within InfoTooltip.tsx to prevent stale closures.3. Bundle & Architectural Cleanup (Medium/Minor)Dependency Audit: Remove react-activity-calendar, react-calendar, and date-fns if they are no longer used after our custom rewrites.CSS Extraction: Move the 74-line inline <style> block in AnalyticsDashboard.tsx into a dedicated CSS module or global stylesheet.Refactor Quests: Clean up the duplicate getOrCreateCharacter() calls in quests.ts and replace the hardcoded 110,000 stat cap in Dashboard.tsx with a shared constant.Cosmetic Cleanup: Remove the unused calDate state and requirements.yaml file.Constraint: Maintain all existing UI themes and "Shadow Leveling" aesthetics. Ensure that after these fixes, the 30-year scheduling window remains fully functional."