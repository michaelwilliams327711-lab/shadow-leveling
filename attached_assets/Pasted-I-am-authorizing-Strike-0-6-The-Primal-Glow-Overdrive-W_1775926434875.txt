I am authorizing Strike 0.6: The Primal Glow Overdrive. We are increasing the visual weight of the Amber Corruption for a specific domain. Execute exactly as specified:

1. CSS Enhancement (artifacts/shadow-leveling/src/index.css):
Add this high-intensity class to the .celestial-void-context section:

CSS
/* --- PRIMAL AMBER OVERDRIVE --- */
.celestial-void-context .primal-glow {
  filter: drop-shadow(0 0 15px hsl(45 100% 60% / 0.8));
  border-color: hsl(45 100% 70% / 0.6) !important;
  z-index: 50;
}

.celestial-void-context .primal-glow .tug-bounce {
  animation: siegePulse 0.8s ease-in-out infinite !important; /* Faster pulse */
  filter: brightness(1.5) contrast(1.2);
}

.celestial-void-context .primal-text {
  text-shadow: 0 0 20px hsl(45 100% 60% / 1);
  letter-spacing: 0.4em;
}
2. Component Targeted Logic (artifacts/shadow-leveling/src/pages/CelestialDuel.tsx):
Modify the DomainCard mapping so we can 'overcharge' a specific domain.

The Target: Let's apply this to 'Sloth vs. Diligence' (or whichever one you're focusing on right now).

Logic: Inside the DOMAIN_PAIRS.map function, add a conditional check:

TypeScript
{DOMAIN_PAIRS.map((pair) => {
  const pairName = `${pair.vice} vs. ${pair.virtue}`;
  // TARGET: Increase the glow for this specific domain
  const isHighIntensity = pair.pair === "sloth_diligence"; 

  return (
    <div 
      key={pairName} 
      className={`relative transition-all duration-500 ${isHighIntensity ? 'primal-glow scale-[1.02]' : ''}`}
    >
      <DomainCard 
        pair={pair} 
        power={powerMap[pairName]} 
        onLog={handleLog} 
        isLogging={isLogging}
        isHighIntensity={isHighIntensity} 
      />
    </div>
  );
})}
3. Particle Intensity:
In the Particle component, if isHighIntensity is true, increase the Math.random() multiplier for the count or the speed of the amber flicker.

'Sovereign Format' Reporting Protocol (REQUIRED):

NO SUMMARIES: I need the full updated source code for CelestialDuel.tsx and the index.css additions.

MARKDOWN HEADERS: Every file must begin with a clear ### FILE: [Full/Path/To/File] header.

RAW CODE BLOCKS: Wrap all code in triple-backtick Markdown blocks.

FINAL INVENTORY: End with an 'Inventory Check' confirming the targeted glow is active."